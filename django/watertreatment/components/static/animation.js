class WaterBar {
	constructor(x, y, w, h, maxCapacity, colour) {
		this.x = x;
		this.y = y;
		this.w = w;
		this.h = h;
		this.maxCapacity;
		this.maxHeight = h;
		this.colour = colour;
	}
}

	// // Grey pipes
	// ctx.strokeStlye = 'grey';
	// ctx.strokeRect(130, 180, 20, 20);
	// ctx.strokeRect(250, 180, 20, 20);
	// ctx.strokeRect(370, 180, 20, 20);

	// // Blue pipes
	// ctx.strokeStyle = "blue";
	// ctx.strokeRect(110, 110, 20, 100);
	// ctx.strokeRect(230, 110, 20, 100);
	// ctx.strokeRect(350, 110, 20, 100);
	// ctx.strokeRect(470, 110, 20, 100);

	// // Purple pipes
	// ctx.strokeStyle = "purple";
	// ctx.strokeRect(150, 180, 80, 20);
	// ctx.strokeRect(270, 180, 80, 20);
	// ctx.strokeRect(390, 180, 80, 20);