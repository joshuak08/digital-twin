# File to configure continuous deployment
# Will be triggered once there is a push to github
# This is for Google Cloud Run through Google Cloud Build
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      ["build", "-t", "gcr.io/$PROJECT_ID/django-testing:latest", "django/watertreatment"]

  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/django-testing:latest"]

  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "django-testing",
        "--image",
        "gcr.io/$PROJECT_ID/django-testing:latest",
        "--region",
        "europe-west1",
        "--platform",
        "managed",
        "--allow-unauthenticated",
        "--port=8000"
      ]

# Need to find another way for Google App Engine for it to interact with creating new database